root: true

exclude:
  - "**/__pycache__/**"
  - .git/**
  - .venv/**
  - "*.egg-info/**"
  - .pytest_cache/**
  - .ruff_cache/**
  - .mypy_cache/**
  - output/**
  - legacy/**

rules:
  deploy-action: false

  mandatory-linters:
    - structurelint
    - pytest-linter

  max-depth:
    max: 5
    exemptions:
      - ".git/**"
      - "**/__pycache__/**"

  max-files-in-dir:
    max: 50
    exemptions:
      - ".git/**"
      - "tests/**"

  max-subdirs:
    max: 15
    exemptions:
      - ".git/**"

  naming-convention:
    style: "snake_case"
    apply-to:
      - "**/*.py"
    exemptions:
      - "**/__pycache__/**"
      - ".git/**"
      - "README.md"
      - "LICENSE"

  file-existence:
    "README.md":
      rule: "exists:1"
      exemptions:
        - "src/**"
        - "tests/**"
        - "schemas/**"
        - "scripts/**"
        - "workflows/**"
        - "docs/**"
    "pyproject.toml":
      rule: "exists:1"
      exemptions:
        - "src/**"
        - "tests/**"
        - "schemas/**"

  disallowed-patterns:
    - "*.tmp"
    - "*.bak"
    - ".DS_Store"
    - "*.pyc"
    - "*.pyo"

  test-adjacency:
    enabled: false
