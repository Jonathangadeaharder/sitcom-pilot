[project]
name = "sitcom-pilot"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "mutmut>=3.5.0",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
]
